import { FunctionComponent } from "react";
import * as React from "react";
import { Box } from "@mui/material";

export const WorkoutsComponent: FunctionComponent = () => (
    <Box>
        Workouts
    </Box>
);
