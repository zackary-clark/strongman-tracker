import { Box } from "@mui/material";
import * as React from "react";
import { FunctionComponent } from "react";

export const AddWorkout: FunctionComponent = () => {
    return (
        <Box>
            Some test
        </Box>
    );
};
