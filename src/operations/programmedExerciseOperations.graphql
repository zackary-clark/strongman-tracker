query ProgrammedExercise($input: SingleProgrammedExerciseInput!) {
    programmedExercise(input: $input) {
        id
        programmedWorkout
        order
        exercise {
            id
            name
            description
            focusGroups
        }
    }
}