"""
Date in yyyy-MM-dd format
"""
scalar Date

interface MutationPayload {
    success: Boolean!
}

interface DeletePayload {
    success: Boolean!
    id: ID!
}

type Query

type Mutation

enum Sort {
    asc
    desc
}
